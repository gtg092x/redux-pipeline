import noramlizeArg from './normalizeArg';
import pipeline from './pipeline';
import withRedux from './pipelineRedux';

describe('Normalize Arg', noramlizeArg);
describe('Pipeline', pipeline);
describe('With Redux', withRedux);
